(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.OtokubiCore=api;})(typeof self!=='undefined'?self:this,function(){
  const KNOWN_ALIASES={
    'joshin':['Joshin','Joshin web','ジョーシン','上新電機','上新'],
    'yamada-denki':['ヤマダデンキ','ヤマダ電機','YAMADA','ヤマダデンキ Yahoo!店','ヤマダ電機 Yahoo!店']
  };
  function norm(s){return(s||'').toLowerCase().normalize('NFKC').replace(/[\s\-_・･/／]+/g,'')}
  function slugFromInput(s){try{const u=new URL(s);if(u.hostname.toLowerCase()==='store.shopping.yahoo.co.jp')return u.pathname.split('/').filter(Boolean)[0]?.toLowerCase()||''}catch(e){}return''}
  function uniq(xs){const out=[],seen=new Set();for(const x of xs||[]){const v=String(x||'').trim(),k=norm(v);if(v&&k&&!seen.has(k)){seen.add(k);out.push(v)}}return out}
  function rowObj(c){if(Array.isArray(c)){const name=c[0]||'',slug=String(c[1]||'').toLowerCase(),extra=Array.isArray(c[2])?c[2]:[];return{name,slug,aliases:uniq([...extra,...(KNOWN_ALIASES[slug]||[])])}}const name=c?.name||'',slug=String(c?.slug||'').toLowerCase(),extra=Array.isArray(c?.aliases)?c.aliases:[];return{name,slug,aliases:uniq([...extra,...(KNOWN_ALIASES[slug]||[])])}}
  function namesFor(r){return uniq([r.name,...(r.aliases||[])])}
  function matchKind(r,q){const nq=norm(q);if(!nq)return null;const vals=namesFor(r).map(v=>({v,n:norm(v)}));if(norm(r.name)===nq)return'exact-name';if(vals.some(x=>x.n===nq))return'exact-alias';if(vals.some(x=>x.n.startsWith(nq)||nq.startsWith(x.n)))return'prefix';if(vals.some(x=>x.n.includes(nq)||nq.includes(x.n)))return'partial';if(r.slug&&norm(r.slug).includes(nq))return'slug';return null}
  function rankKind(k){return({'exact-name':100,'exact-alias':98,'prefix':85,'partial':70,'slug':60})[k]||0}
  function searchCatalog(catalog,query,limit=8){const q=String(query||'').trim(),qslug=slugFromInput(q);if(!q)return[];const out=[];for(let i=0;i<(catalog||[]).length;i++){const r=rowObj(catalog[i]);let kind=null,score=0;if(qslug){if(r.slug!==qslug)continue;kind='exact-slug';score=120}else{kind=matchKind(r,q);if(!kind)continue;score=rankKind(kind)}out.push({...r,id:i,kind,score})}out.sort((a,b)=>b.score-a.score||a.name.localeCompare(b.name,'ja'));return out.slice(0,Math.max(1,limit||8))}
  function rowsFor(day,catalog){if(Array.isArray(day?.stores))return day.stores.map(s=>({...rowObj(s),rate:Number(s.rate)}));if(Array.isArray(day?.offers)&&Array.isArray(catalog))return day.offers.map(o=>({...rowObj(catalog[o[0]]||[]),rate:Number(o[1])}));return[]}
  function indexedMatches(day,shop,catalog){const q=norm(shop),qslug=slugFromInput(shop);if(!q&&!qslug)return{empty:true,hits:[]};const offers=day?.offers||[],exactSlug=[],exactText=[],partial=[];for(const o of offers){const c=rowObj(catalog?.[o[0]]||[]),rate=Number(o[1]),row={...c,rate};if(qslug&&c.slug===qslug){exactSlug.push(row);continue}if(!qslug&&q){const kind=matchKind(c,shop);if(kind==='exact-name'||kind==='exact-alias'){exactText.push(row);continue}if(kind)partial.push(row)}}if(exactSlug.length)return{hits:exactSlug};if(exactText.length){const identities=new Set(exactText.map(s=>s.slug||norm(s.name)));if(identities.size>1)return{ambiguous:true,hits:exactText.slice(0,8)};return{hits:exactText}}if(partial.length){const identities=new Set(partial.map(s=>s.slug||norm(s.name)));if(identities.size>1)return{ambiguous:true,hits:partial.slice(0,8)};return{hits:partial}}return{hits:[]}}
  function matchRows(day,shop,quality,catalog){if(Array.isArray(day?.offers)&&Array.isArray(catalog)){const m=indexedMatches(day,shop,catalog);if(m.empty)return{rate:null,state:'empty'};if(m.ambiguous)return{rate:null,state:'ambiguous',matches:m.hits};if(m.hits.length){m.hits.sort((a,b)=>b.rate-a.rate);return{rate:m.hits[0].rate,state:'match',store:m.hits[0],quality}}return quality==='ok'?{rate:0,state:'not_found'}:{rate:null,state:'uncertain'}}const q=norm(shop),qslug=slugFromInput(shop);if(!q&&!qslug)return{rate:null,state:'empty'};const rows=rowsFor(day,catalog);let hits=qslug?rows.filter(s=>s.slug===qslug):[];if(!hits.length&&q){const exact=rows.filter(s=>{const k=matchKind(s,shop);return k==='exact-name'||k==='exact-alias'}),ids=new Set(exact.map(s=>s.slug||norm(s.name)));if(ids.size>1)return{rate:null,state:'ambiguous',matches:exact.slice(0,8)};hits=exact}if(!hits.length&&q){const partial=rows.filter(s=>!!matchKind(s,shop)),ids=new Set(partial.map(s=>s.slug||norm(s.name)));if(ids.size>1)return{rate:null,state:'ambiguous',matches:partial.slice(0,8)};hits=partial}if(hits.length){hits.sort((a,b)=>Number(b.rate)-Number(a.rate));return{rate:Number(hits[0].rate),state:'match',store:hits[0],quality}}return quality==='ok'?{rate:0,state:'not_found'}:{rate:null,state:'uncertain'}}
  function shopRate(day,shop,catalog){if(!day)return{rate:null,state:'missing'};if(day.status==='partial')return matchRows(day,shop,'partial',catalog);if(day.status!=='ok')return{rate:null,state:'error'};return matchRows(day,shop,'ok',catalog)}
  function safeCampaignTitle(title){const t=String(title||'').toLowerCase();if(!t)return false;return !['クーポン','くじ','抽選','対象商品購入','ギフトで贈る','ebookjapan','zozotown'].some(x=>t.includes(x.toLowerCase()))}
  function eventsFor(iso,campaigns){
    let out=[];const d=new Date(iso+'T00:00:00');if([5,15,25].includes(d.getDate()))out.push({title:'5のつく日',rate:4,rate_label:'+4%',entry_required:true,target_store_limited:false,eligibility_rule:'all',informational:false,rankable:true,kind:'fixed'});
    const dynamic=[];for(const c of campaigns?.campaigns||[])if((safeCampaignTitle(c.title)||c.informational)&&(c.dates||[]).includes(iso))dynamic.push({title:c.title,period:c.period,rate:c.rate==null?null:Number(c.rate),rate_label:c.rate_label||null,is_total:!!c.is_total,is_max:!!c.is_max,informational:!!c.informational,rankable:c.rankable!==false,conditions:Array.isArray(c.conditions)?c.conditions:[],entry_required:!!c.entry_required,target_store_limited:!!c.target_store_limited,eligibility_rule:c.eligibility_rule||(!c.target_store_limited?'all':'campaign_target_store'),source:c.source||campaigns?.source||null,source_url:c.source_url||c.source||campaigns?.source||null,kind:'guide'});
    const blocksFirstDay=dynamic.some(x=>x.title.includes('プレミアムな日曜日')||x.title.includes('爆買WEEK')||x.title.includes('Brand Week')||x.title.includes('ブランドウィーク'));if(d.getDate()===1&&!blocksFirstDay)out.push({title:'ファーストデイ',rate:3,rate_label:'+3%',entry_required:true,target_store_limited:false,eligibility_rule:'all',informational:false,rankable:true,kind:'fixed'});out.push(...dynamic);
    const merged=[],pos=new Map();for(const x of out){const k=norm(x.title)+(x.rate_label||'');if(!pos.has(k)){pos.set(k,merged.length);merged.push(x);continue}const i=pos.get(k);if(merged[i].kind==='fixed'&&x.kind==='guide')merged[i]=x}return merged.slice(0,10)
  }
  function rateKey(rate){const n=Number(rate);return Number.isFinite(n)?(Number.isInteger(n)?String(n):String(n)):''}
  function bonusCapInfo(rate,bonusData){const r=Number(rate),key=rateKey(r),raw=bonusData?.rate_caps?.[key],cap=Number(raw);if(key&&Number.isFinite(cap)&&cap>0)return{known:true,cap,source:'official'};if(r===10)return{known:true,cap:10000,source:'legacy'};if(r===5)return{known:true,cap:5000,source:'legacy'};return{known:false,cap:null,source:'unknown'}}
  function fmtRate(r){return Number.isInteger(r)?String(r):String(r).replace(/\.0+$/,'')}
  return {norm,slugFromInput,rowObj,namesFor,matchKind,searchCatalog,rowsFor,indexedMatches,matchRows,shopRate,safeCampaignTitle,eventsFor,rateKey,bonusCapInfo,fmtRate};
});